package cpup.mc.tweak.content.assistant

import net.minecraft.entity.player.EntityPlayer
import net.minecraft.util.ResourceLocation

class CraftingWing extends Wing {
	override def texture: ResourceLocation = new ResourceLocation("fizbuz")

	override def click(player: EntityPlayer) = {

	}
}
